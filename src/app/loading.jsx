function loading() {
  return (
    <div
      style={{
        textAlign: "center",
        margin: "150px 0",
        fontSize: "40px",
        fontWeight: "700",
        minHeight: "100vh",
      }}
    >
      Loading...
    </div>
  );
}

export default loading;
